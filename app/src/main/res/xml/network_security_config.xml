<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Note: base-config must allow cleartext because HLS video streams
         come from unknown CDN domains over HTTP. Domain-specific configs
         here serve as documentation, not as a blocklist. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">ikanbot.com</domain>
        <domain includeSubdomains="true">ikanbot.tv</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
